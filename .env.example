# --- Frontend (Vite) -------------------------------------------------
# Leave blank to run fully on bundled MOCK data (zero setup), including
# a computed AI analysis and comparison so nothing looks empty.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_USE_MOCK=false

# --- Supabase Edge Function secrets (set with: supabase secrets set) ---
# Financial data (US fundamentals). Free key: https://site.financialmodelingprep.com
FMP_API_KEY=

# AI analyst + comparator narrative. Free key: https://aistudio.google.com/apikey
# If omitted, the app still works using the deterministic computed analysis.
GEMINI_API_KEY=
